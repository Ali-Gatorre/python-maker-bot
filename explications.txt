creation de plusieurs fichier pour decouper le code:

-api.rs             ->pour les appels a l'API Hugging face
-python_exec.rs     ->pour exécuter le code python
-interface.rs       -> pour l'interface du bot (CLI)

on fait juste les fonctions dans le bon fichier LES METTRES PUBLIQUES
(ex pub fn test(){} dans api.rs)
et on appel dans le main: api::test();

pour les fonctions asynchrones 
pub async fn test(){} dans api.rs
Dans le main: api::test().await;  //ne pas oublier await

si vous vouler utiliser vos fonction dans un autre fichier que le main rajouttez juste "mod api;" en haut du fichier